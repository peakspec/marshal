---
name: test-scenarios
description: EXTERNAL SKILL NOT YET VENDORED — original source: ~/.claude/skills/test-scenarios/SKILL.md (installed via npx skills; not present on this machine)
---

# test-scenarios

## PLACEHOLDER — not vendored

This skill is referenced by the `minions-qa` agent's SKILLS table but was not found
in the repo (`skills/` dir) nor at `~/.claude/skills/test-scenarios/SKILL.md`. It is
distributed as an external skill (installed via `npx skills`) and was not installed on
the machine at port time.

## To vendor

1. Install the skill: `npx skills install test-scenarios` (or whatever registry command applies).
2. Copy `~/.claude/skills/test-scenarios/SKILL.md` (and any `references/` subdir) over this file.
3. Replace this placeholder body with the real skill content.

## Expected purpose (from minions-qa.md)

Happy paths, edge cases, error handling. Produces a full test-scenario set per feature,
mapping acceptance criteria to tests.
