---
name: webapp-testing
description: EXTERNAL SKILL NOT YET VENDORED — original source: not installed; minions-qa.md marks this "Skill not yet installed"
---

# webapp-testing

## PLACEHOLDER — not vendored

The `minions-qa` agent lists this skill under "Skill not yet installed" and instructs the
agent to work from training knowledge if invoked. It was not found in the repo (`skills/`
dir) nor at `~/.claude/skills/webapp-testing/SKILL.md`. No external install source is
recorded in the agent definition.

## To vendor

1. Locate or author the skill (Playwright-based web app testing).
2. Copy its `SKILL.md` (and supporting assets) over this file.
3. Replace this placeholder body with the real skill content.

## Expected purpose (from minions-qa.md)

Web application testing with Playwright. Produces Playwright test scripts (key output of
the QA agent when this skill is invoked).
